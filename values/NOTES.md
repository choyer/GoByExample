# Values NOTES

- `fmt.Println` automatically appends *newline* (\n)
- Formats operands using their default Go formats.
- Does NOT support specific format specifiers (like %s or %d). Use `fmt.Printf` for that functionality.
