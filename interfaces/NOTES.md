#interfaces NOTES

- An interface is ***a type that defines a set of method signatures***. It's interface implementation is **implicit**, meaning a type does NOT need to explicitly declare that it implements an interface; it just needs to have all the required methods.
